import React from 'react'
import { Form, FormGroup, Input, Label } from 'reactstrap';
import banner from './resort_background.jpg';

export const Home = () => {
  return (
    <div>
    </div>
  )
}

export default Home;
